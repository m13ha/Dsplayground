import { Box, Typography } from "@mui/material";

interface Props {
}

const IntroParagraph = (props: Props) => {
    return (
        <Box>
            <Typography variant="body1" color="initial">
                
            </Typography>
        </Box>
    );
};

export default IntroParagraph;